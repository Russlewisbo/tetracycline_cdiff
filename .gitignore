# Quarto / R
/.quarto/
**/*.quarto_ipynb
.Rproj.user
.posit/assistant
.Rhistory
.RData

# Generated build artifacts (never track)
/report_cache/
/report_files/
# NOTE: _freeze/ is intentionally committed so CI/site rebuilds reuse frozen results

# Stray root render artifacts (regenerated; not part of the published site)
/report.html
/report.rmarkdown
/render_rerun*.log
/Preview_*.pdf
